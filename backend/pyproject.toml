[project]
name = "botbet-clash-backend"
version = "0.1.0"
description = "AI Prop Betting League backend (Phase 1: domain skeleton)"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
